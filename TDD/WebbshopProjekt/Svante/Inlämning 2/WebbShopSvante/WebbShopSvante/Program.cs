using System;

namespace WebbShopAPI
{
    internal static class Program
    {
        private static void Main(string[] args)
        {
            // sparar lite bortkomenterad kod i main för jag tror jag vill ha det som referens senare
            //TODO: ta bortkomenterade foreach från metoder och flyttar till views i nya metoder
             // Seeder.Seed();

           // int userId = WebbShopApi.Login("TestCustomer", "Codic2021");
           // WebbShopApi.AddUser(2, "Svante", "svante123");
           // WebbShopApi.AddUser(2, "Glen", "Glen123");
           // WebbShopApi.DeleteCategory(2, 6);
           // WebbShopApi.UpdateGenre(2, 5, "moccumentary");
           // WebbShopApi.UpdateGenre(2, 5, "Documentary");
           // WebbShopApi.AddCategory(2, "Hobby");
           // WebbShopApi.AddBookToCategory(2, 5, 3);
           // WebbShopApi.DeleteCategory(2, 6);

           //// WebbShopApi.UpdateBook(2, 5, "brosk och saft","fjodor doktor-jetski", 55);
           //// WebbShopApi.UpdateBook(2, 5, "Brott och straff", "Fjodor Dostojevskij", 500); test ok

           //// WebbShopApi.DeleteBook(2,1); test ok
           // var ping = WebbShopApi.Ping(1);
           // // WebbShopApi.LogOut(userId);
           // // WebbShopApi.BuyBook(userId, 1);
           // var printUsers = WebbShopApi.ListUsers(2);
           // foreach (var user in printUsers)
           // {
           //     Console.WriteLine(user.Name + " " + user.Password);
           // }
           // //WebbShopApi.AddBook(1, "Brott och straff", "Fjodor Dostojevskij", 500, 2);
           // //WebbShopApi.AddBook(2, "Brott och straff", "Fjodor Dostojevskij", 500, 2);
           // //WebbShopApi.AddBook(2, "Brott och straff", "Fjodor Dostojevskij", 500, 2);

           // WebbShopApi.Register("Glen" , "Glen123" , "Glen123");
           // WebbShopApi.Register("Glen", "Glen123", "Glen123");
           // WebbShopApi.Register("GoaGlen", "Glen123", "123Glen");
            
           // WebbShopApi.GetCategories();
           // WebbShopApi.GetCategories("Horror");
           // WebbShopApi.GetCategory(1);
           // var bookTitle = WebbShopApi.GetBookTitle(1);
           // try
           // {
           //     Console.WriteLine(bookTitle);
           // }
           // catch
           // {
           //     Console.WriteLine("Oops! Den boken fanns visst inte!");
           // }

           // var book = WebbShopApi.GetBook(1);
           // Console.WriteLine(book.ToString());

           // var books = WebbShopApi.GetAuthors("Stephen King");
           // Console.WriteLine("Trying Authors");
           // foreach (var abook in books)
           // {
           //     Console.WriteLine($"{abook.Author} {abook.Title}");

           // }



        }
    }
}